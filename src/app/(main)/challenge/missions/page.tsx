import { ChallengePlaceholderPage } from '@/components/challenge/challenge-placeholder-page';

export default function ChallengeMissionsPage() {
    return (
        <ChallengePlaceholderPage
            breadcrumb="홈 > 챌린지 > 미션"
            title="챌린지 미션"
            description="미션 목록/달성 처리 API 연동 전 기본 라우트입니다."
        />
    );
}
