# Supabase (Required)
NEXT_PUBLIC_SUPABASE_URL=https://api.playbid.kr
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# App URL used by OAuth readiness checks and redirect validation
NEXT_PUBLIC_SITE_URL=https://playbid.kr

# Optional: use when OAuth callback endpoint is served on a separate self-hosted domain
# Example: https://api.playbid.kr/auth/v1/callback
SUPABASE_AUTH_CALLBACK_URL=
OAUTH_DEFERRED_PROVIDERS=apple

# Admin allow list (optional)
ADMIN_ALLOWED_EMAILS=admin@playbid.kr,manager@playbid.kr
